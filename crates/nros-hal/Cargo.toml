[package]
name = "nros-hal"
version.workspace = true
edition.workspace = true
description = "NROS HAL: Unified sensor interface, zero-copy DMA, multi-sensor synchronization, hardware triggers"
license.workspace = true

[lib]
name = "nros_hal"
path = "src/lib.rs"

[[bin]]
name = "nros-hal-demo"
path = "src/main.rs"

[dependencies]
nros-core = { path = "../nros-core" }
nros-types = { path = "../nros-types" }
